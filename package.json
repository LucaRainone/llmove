{
  "name": "llmove",
  "version": "0.2.0",
  "description": "`llmove` is a command-line tool for parsing and transforming XML-based specifications using a language model (Claude Opus by Anthropic).  It recursively scans a directory of `.xml` spec files, extracts user prompt content, and calls a language model API to generate corresponding output files.",
  "main": "llmove.cjs",
  "bin": {
    "llmove": "./llmove.cjs"
  },
  "directories": {
    "example": "examples",
    "lib": "lib"
  },
  "scripts": {

  },
  "author": "Luca Rainone",
  "license": "CC0-1.0"
}
